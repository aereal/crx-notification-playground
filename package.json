{
  "name": "crx-notification-playground",
  "version": "0.0.1",
  "description": "Play with [chrome.notifications API][chrome.notification API].",
  "main": "index.js",
  "scripts": {
    "build": "$(npm bin)/gulp build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "aereal",
  "license": "MIT",
  "devDependencies": {
    "browserify": "^11.0.0",
    "gulp": "^3.9.0",
    "tsd": "^0.6.3",
    "tsify": "^0.11.2",
    "vinyl-source-stream": "^1.1.0"
  }
}
